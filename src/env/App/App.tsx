import type {FC} from 'react';

import {FidgetSpinner} from '@/lib';
import './index.css';

const App: FC = () => {
    return (
        <div>
            <FidgetSpinner />
        </div>
    );
};

export default App;
